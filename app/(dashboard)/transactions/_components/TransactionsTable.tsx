"use client";

import React from "react";

interface Props {
  from: Date;
  to: Date;
}

const TransactionsTable = ({ from, to }: Props) => {
  return <div>TT</div>;
};

export default TransactionsTable;
